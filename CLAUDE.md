# tada — プロジェクト概要

## 一言で言うと

商品名を入力するだけで「本当の値段（実質コスト）」がわかる Web アプリ。

---

## コアコンセプト

**結論ファースト：商品名と実質コストをドカンと見せ、根拠は補足として後に続ける。**

ユーザーが「Leica M11」と入力すると、以下の構成で結果を表示する：

### メイン：結論カード

商品名と実質コスト（購入価格 − 推定売却価格）を大きく表示する。
ユーザーは売却年数スライダーで年数を変えると、実質コストがクライアント側で瞬時に再計算される。

> 例: Leica M11 — 実質コスト ¥168,544（5年後に売却した場合）

### 根拠：エビデンスカード群

結論の根拠として、以下のデータカードを並べる（最大3枚）：
1. **現行モデルカード** — 発売年・販売価格・現在の買取相場
2. **前世代モデルカード** — 発売時価格・現在の買取価格・価値保持率バー

長文の説明テキストは一切不要。カード内はデータのみで構成する。

### 補足情報

- ソースリンク（算出根拠のデータソースへのリンクを控えめに表示）
- 同カテゴリの実質コスト比較（関連商品カード3枚）

---

## 技術スタック

| 項目 | 技術 | 導入フェーズ |
|---|---|---|
| フレームワーク | Next.js 16 (App Router) + TypeScript + Tailwind CSS v4 | v0.0 |
| 価格データ取得 | Perplexity Sonar API（Web 検索 + AI 合成を 1 コールで） | v0.1 |
| AI SDK | Vercel AI SDK (`ai` + `@ai-sdk/perplexity`) | v0.1 |
| スキーマ検証 | Zod（Sonar の出力を構造化） | v0.1 |
| ストリーミング | Vercel AI SDK `streamText`（UX のため必須） | v0.1 |
| ホスティング | Vercel | v0.0（プレビュー）〜 |
| DB | Supabase (PostgreSQL) — 認証・課金用 | v0.2 |
| 認証 | Supabase Auth + Google OAuth | v0.2 |
| 課金 | Stripe（月額サブスクリプション） | v0.2 |
| キャッシュ DB | Supabase (PostgreSQL) — API コスト削減用 | v0.3（後述） |

---

## インフラ情報

| 項目 | 値 |
|---|---|
| GitHub | https://github.com/tkfire0309/tada |
| 本番 URL | https://tada-pi.vercel.app |
| Vercel チーム | takus-projects-42d9f92b |
| Supabase プロジェクト ref | vhnwhqtdpbievnqofoqd |
| Supabase リージョン | ap-northeast-1 (Tokyo) |

---

## 開発方針

### スクラップ & ビルド前提

UI は最初から完成形にならない。最初のプロトタイプは「捨てる前提」で作り、体験を確認してから本実装に進む。
そのため、以下の原則を守る：

- **UI プロトタイプ（v0.0）は専用ブランチで行い、main には入れない**
- **v0.0 で学んだことをもとに、v0.1 でゼロから作り直す可能性がある**
- **v0.0 段階ではファイル構成・命名にこだわりすぎない**（後で捨てるため）
- **ただし型定義（types.ts）だけは v0.0 から丁寧に作る**（API 連携時に再利用するため）

### Git ブランチ戦略

```
main                    ← 本番デプロイ対象。常にクリーンな状態を維持
  │
  ├── prototype/ui      ← v0.0 UIプロトタイプ（捨てる前提）
  │
  ├── dev               ← v0.1 以降の開発ブランチ（main にマージしていく）
  │     ├── feat/xxx    ← 機能単位のブランチ
  │     └── fix/xxx     ← 修正単位のブランチ
  │
  └── (将来) release/x.x
```

**運用ルール:**
- `main` には直接コミットしない（常に PR 経由 or ブランチからマージ）
- `prototype/ui` は実験用。気に入らなければ `git branch -D prototype/ui` で丸ごと削除
- v0.0 完了後、学びを踏まえて `dev` ブランチで本実装を開始
- 各フェーズの切り替え時は新しいブランチを切る（ロールバック容易）

**やり直しの手順:**
```bash
# プロトタイプを捨てて最初からやり直す場合
git checkout main
git branch -D prototype/ui
git checkout -b prototype/ui-v2
# → ゼロから再実装
```

---

## バージョン別ロードマップ

### v0.0 — UI プロトタイプ（完了・以下は確定した UI 仕様）

**目的:** ユーザー体験の確認。API 連携・DB・認証は一切なし。
v0.0 プロトタイプは 3 回のレビューを経て UI 構造が確定した。以下は最終形の仕様であり、v0.1 以降で UI を再構築する際の「説明書」として機能する。

**ブランチ:** `prototype/ui`

**v0.0 で確定した重要な設計判断:**

1. **結論ファースト** — 検索結果の最上部に「商品名 + 実質コスト」を大きく表示。根拠データは下部に補足として配置。
2. **年数非依存の API データ設計** — API は年数に依存しない基礎データ（販売価格・買取価格・前モデル実績・年間減価率）を返す。年数に応じた実質コスト計算はクライアント側で行う。
3. **買取価格の重視** — 中古販売相場ではなく「買取価格」を表示する。ユーザーが実際に手にする金額が買取価格だから。
4. **検索フォームは商品名のみ** — 売却予定年数は検索フォームから削除。結果カード内のスライダーで変更する。
5. **v0.0 の計算ロジックは仮** — 単純な指数減価モデルを使っているが、実際の価値下落は非線形。v0.1 では Perplexity から取得した多角的データに基づく計算ロジックをゼロから設計すること。

#### 結果表示の UI 構造（ResultStream）

結果表示は 2 フェーズで構成される：

**フェーズ 1: 分析中（analyzing）**
```
┌─────────────────────────────────┐
│ [スピナー] 分析中... (3/6)       │
│                                 │
│ ✓ 前世代モデルの情報を検索...    │
│ ✓ Leica M10 の発売時価格を取得   │
│ ✓ 買取相場を調査しています...    │
└─────────────────────────────────┘
```
- 分析ステップを 1 つずつフェードインで表示（setInterval 800ms）
- 全ステップ完了後 600ms でフェーズ 2 に遷移

**フェーズ 2: 結果表示（done）— 上から順に以下の要素を配置**

**(A) メインカード** — 結論を大きく表示
```
┌─────────────────────────────────────┐
│ 5年後に売却した場合     [価格を編集] │
│                                     │
│         Leica M11                   │
│                                     │
│          実質コスト                  │
│       ¥168,544  ← 4xl bold accent   │
│   [AIの算出結果に戻す] ← 編集時のみ  │
│                                     │
│ ─────────────────────────────────── │
│  購入価格    推定売却価格   年間コスト │
│ ¥1,280,000   ¥1,111,456   ¥33,709  │
│                            /年      │
│ ─────────────────────────────────── │
│ 売却予定年数                   5年   │
│ ●━━━━━━━━━━━━━━━━━━━━━━━━━━━━○  │
│ 1年          10年          20年     │
└─────────────────────────────────────┘
```
- **実質コスト** = 購入価格 − 推定売却価格。アクセントカラー・4xl・bold で表示。
- **3 列サマリー** = 購入価格 / 推定売却価格 / 年間コスト。border-top で区切り。
- **価格編集モード** = 「価格を編集」ボタンで購入価格・推定売却価格が input に切り替わる。変更すると実質コストが即再計算。「AIの算出結果に戻す」で元に戻せる。
- **年数スライダー** = range input（1〜20年）。変更するとクライアント側で推定売却価格・実質コスト・年間コストを再計算（API 再呼び出しなし）。年数変更時はカスタム売却価格をリセット（購入価格の手動編集は保持）。

**(B) 根拠カード群**

「根拠」ラベルの下に最大 3 枚のカード。長文テキストは一切不要、データのみ。

**カード 1: 現行モデルの市場価格**
```
┌─────────────────────────────────┐
│ Leica M11              現行モデル│
│                                 │
│ ┌─────┐ ┌─────────┐ ┌────────┐ │
│ │発売年 │ │ 販売価格 │ │買取相場 │ │
│ │2022年│ │¥1,280,000│ │¥1,100,000│ │
│ └─────┘ └─────────┘ └────────┘ │
└─────────────────────────────────┘
```

**カード 2: 前世代モデルの実績**
```
┌─────────────────────────────────┐
│ Leica M10            前世代モデル│
│                                 │
│ ███████████████░░░░░  75%保持   │ ← 価値保持率バー
│                                 │
│ ┌────────┐ ┌──────────┐ ┌──────┐│
│ │発売時価格│ │8年後の買取│ │実質  ││
│ │¥1,000,000│ │¥750,000  │ │¥250,000││
│ └────────┘ └──────────┘ └──────┘│
└─────────────────────────────────┘
```

**(C) ソースリンク** — 根拠カード群の下に控えめに表示（10px、underline、muted）

**(D) 関連商品カード** — 「同カテゴリの実質コスト」ラベルの下に 3 列グリッド
```
┌──────┐ ┌──────┐ ┌──────┐
│Sony   │ │Canon  │ │Nikon  │
│α7 IV  │ │EOS R5 │ │Z8     │
│¥45,000│ │¥52,000│ │¥48,000│
│ /年   │ │ /年   │ │ /年   │
│カメラ  │ │カメラ  │ │カメラ  │
└──────┘ └──────┘ └──────┘
```

#### 検索フォーム（SearchForm）

- **商品名のみ**（売却予定年数の入力欄は結果カード側に移動済み）
- テキスト入力 + サジェストドロップダウン（ハードコード）
- 「調べる」ボタン + Enter 送信対応
- プレースホルダー: 「商品名を入力（例: iPhone 16 Pro）」

#### API データ構造（AnalysisBaseData）

API は以下の年数非依存データを返す。年数に応じた計算はクライアント側の `calcEstimate()` で行う：

```typescript
interface AnalysisBaseData {
  productName: string;          // 検索対象商品名
  currentPriceYen: number;      // 現在の販売価格
  currentBuybackPriceYen: number; // 現行モデルの現在の買取相場
  releaseYear: number;          // 現行モデルの発売年
  annualDepreciationRate: number; // 年間減価率（0〜1）前モデル実績から算出
  previousModel: {
    modelName: string;
    releaseYear: number;
    releasePriceYen: number;    // 発売時価格
    currentBuybackPriceYen: number; // 現在の買取相場
    yearsElapsed: number;
    retentionRate: number;      // 価値保持率（%）
  };
  citations: { title: string; url: string }[]; // 情報源（タイトル+URL）
}
```

**重要: v0.0 の `calcEstimate()` は `purchasePrice * (1 - rate)^years` の単純な指数減価モデル。実際の価値下落は非線形（発売直後に急落 → 緩やかに安定）なので、v0.1 で Perplexity API から取得したデータに基づく計算ロジックをゼロから設計すること。**

**ファイル構成:**
```
src/
  app/
    page.tsx                    -- トップページ（ヒーロー + 検索 + フィード + FAQ + CTA）
    terms/page.tsx              -- 利用規約（タブ切り替え: 利用規約 / プライバシーポリシー）
    pricing/page.tsx            -- プラン料金説明（2カラム比較カード）
    about/page.tsx              -- サービスについて（ストーリー形式）
    changelog/page.tsx          -- 更新情報（日付降順リリースノート）
    faq/page.tsx                -- FAQ（カテゴリ別アコーディオン）
    contact/page.tsx            -- お問い合わせ（メールリンク + テンプレート）
    layout.tsx                  -- 共通レイアウト（ヘッダー + フッター）
  components/
    Header.tsx                  -- ヘッダー（ロゴ + ナビ）
    Footer.tsx                  -- フッター（3カラム リンク集）
    SearchForm.tsx              -- 商品名入力 + サジェスト（年数入力なし）
    ResultStream.tsx            -- 結果表示（分析アニメ → 結論カード → 根拠カード）
    FeedCarousel.tsx            -- 検索フィードの横スクロールカード
    FeedCard.tsx                -- フィード内の個別カード
    FaqAccordion.tsx            -- FAQ アコーディオン（トップページ抜粋 + /faq 共用）
    PricingCard.tsx             -- 料金プラン比較カード（/pricing 用）
  lib/
    types.ts                    -- 共通型定義（★ v0.1 でも再利用するため丁寧に作る）
    mock-data.ts                -- ダミーデータ集約（フィード、サジェスト、検索結果）
```

**追加パッケージ:** なし（Tailwind CSS のみで実装。v0.0 では外部ライブラリを増やさない）

### v0.1 — MVP（API 連携）

v0.0 の学びをもとに、Perplexity Sonar API を接続して「本当に動くもの」を作る。
v0.0 のコードをそのまま使うか、ゼロから作り直すかは v0.0 の結果次第。

**ブランチ:** `dev`（main から新規作成）

**スコープ:**
- Perplexity Sonar API 連携（ストリーミング対応）
- Vercel AI SDK (`streamText`) による実ストリーミング表示
- 計算ロジック実装（実質コスト・年間コスト）
- Zod によるレスポンススキーマ検証
- Vercel 本番デプロイ

**v0.0 → v0.1 で変わる部分:**
- `ResultStream.tsx`: setTimeout ダミー → Vercel AI SDK `useChat` / `useCompletion` に差し替え
- `SearchForm.tsx`: クライアント処理 → API Route 呼び出しに変更
- `api/analyze/route.ts`: 新規追加（Sonar API 呼び出し + ストリーミング）
- `lib/perplexity.ts`: 新規追加（API クライアント + プロンプト）
- `lib/calc.ts`: 新規追加（計算ロジック）

**必要なパッケージ（v0.1 で追加）:**
```
ai @ai-sdk/perplexity zod
```

**環境変数:**
```
PERPLEXITY_API_KEY=  # ユーザーが取得して設定する
```

### v0.2 — 認証 + 課金 + プラン別モデル切り替え

ユーザー管理とマネタイズを導入する。

**スコープ:**
- Supabase Auth（Google OAuth）によるログイン
- Stripe による月額課金（無料 / Pro の 2 プラン）
- プランに応じた API モデル切り替え（後述の料金プラン参照）
- 日次利用回数のレート制限
- フィードカードを実データ（実際の検索結果）に切り替え
- サジェストを実データ（検索ログ）に切り替え

**追加ファイル（想定）:**
```
src/
  app/
    login/page.tsx              -- ログインページ
    api/webhook/stripe/route.ts -- Stripe Webhook 受信
  lib/
    supabase.ts                 -- Supabase クライアント（認証用）
    stripe.ts                   -- Stripe クライアント
    rate-limit.ts               -- レート制限ロジック
```

### v0.3 — キャッシュ DB（API コスト最適化）

Perplexity API のコスト削減のため、検索結果を Supabase にキャッシュする。

**背景:**
- 同じ商品（例: iPhone 17）を複数ユーザーが検索した場合、毎回 API を叩くのは非効率
- 一度取得した結果を DB に保存し、TTL（例: 7 日間）以内なら DB から返すことで API コールを削減
- ユーザーにとっては API 直接呼び出しか DB 返却かは区別できない

**アーキテクチャ:**
```
ユーザーのリクエスト
        │
  [Next.js API Route]
        │
  Supabase にキャッシュあり？（TTL 以内か？）
    │ Yes              │ No
  DB から返す       Perplexity API を叩く
                        │
                   DB に保存（TTL 付き）
                        │
                     結果を返す
```

**課題（将来検討）:**
- 中古相場は変動するため TTL の適切な設定が必要（7 日が目安）
- 検索キーの正規化（「iPhone17」「iphone 17」「アイフォン17」を同一視する仕組み）
- キャッシュヒット率はデータ蓄積に比例して向上する（初期 50% → 半年後 70-80%）

---

## 料金プラン

### プラン構成

| プラン | 月額 | 1 日の上限 | 使用モデル |
|---|---|---|---|
| 無料 | 0 円 | 5 回 | sonar |
| Pro | 980 円 | 100 回（2 段階制） | sonar-pro + sonar |

### Pro プランの 2 段階モデル切り替え

Pro プランは 1 日の利用を 2 つのステージに分ける：

| ステージ | 回数 | モデル | 特徴 |
|---|---|---|---|
| ステージ 1 | 1〜30 回目 | sonar-pro | 高精度な検索結果 |
| ステージ 2 | 31〜100 回目 | sonar | 標準精度の検索結果 |

### レート制限の実装方針

- **未ログインユーザー（無料）:** IP アドレスベースで日次カウント
- **ログインユーザー（Pro）:** ユーザー ID ベースで日次カウント
- カウント管理は Supabase DB + API Route 内で処理

### API コスト参考値

| モデル | 1 回あたりコスト |
|---|---|
| sonar | 約 1 円（入力 + 出力 + 検索料） |
| sonar-pro | 約 3.6 円（入力 + 出力 + 検索料） |

---

## UI 設計

デザインの方向性は NANI（https://nani.now/ja）を参考にする。
ミニマル・洗練・シンプルを最優先とし、複雑化しない。

### デザインシステム

- **配色:** 白背景ベース + アクセントカラー（ゴールド/イエロー系）+ ダークモード対応
- **角丸:** カード・ボタンに大きめの border-radius
- **シャドウ:** カードに軽いドロップシャドウで奥行きを出す
- **アニメーション:** フェードイン、ホバー時のスケール変化（派手にしない）
- **レスポンシブ:** モバイルファースト

### トップページ構成（上から順に）

```
┌─────────────────────────────────────┐
│  Header（ロゴ + 最小限のナビ）        │
├─────────────────────────────────────┤
│                                     │
│  Hero セクション                     │
│  「本当の値段、知ってる？」           │
│  サブテキスト                        │
│                                     │
│  検索フォーム（商品名のみ・中央配置） │
│  [商品名を入力___________] [調べる]  │
│  ┌──────────────┐                   │
│  │ サジェスト候補 │                   │
│  └──────────────┘                   │
│                                     │
│  結果表示エリア                      │
│  ┌─────────────────────────────┐    │
│  │ (A) メインカード（結論）     │    │
│  │ (B) 根拠カード群             │    │
│  │ (C) ソースリンク             │    │
│  │ (D) 関連商品カード           │    │
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│  フィードカルーセル                  │
│  「みんなの検索」                    │
│  ← 自動スクロール                   │
├─────────────────────────────────────┤
│  FAQ セクション（3問抜粋）           │
│  「その他のよくある質問 →」          │
├─────────────────────────────────────┤
│  CTA セクション                     │
│  「本当の値段を調べてみよう」        │
│  [無料ではじめる]                    │
├─────────────────────────────────────┤
│  Footer（3カラム構成）               │
└─────────────────────────────────────┘
```

※ 結果表示の (A)〜(D) の詳細は v0.0 セクションの「結果表示の UI 構造」を参照。

### 検索フォーム詳細

- **商品名のみ**の入力フィールド（売却予定年数は結果カード内のスライダーに移動）
- プレースホルダー: 「商品名を入力（例: iPhone 16 Pro）」
- 入力中にサジェストをドロップダウン表示（v0.0 はハードコード、v0.2 で検索ログベースに）
- 「調べる」ボタン + Enter キーで送信

### フィードカード詳細

**カード 1 枚の表示内容:**
- 商品名（例: 「iPhone 15 Pro」）
- 実質コスト（例: 「実質 32,000 円/年」）
- 検索したユーザー名（匿名可。例: 「ユーザーA」）

**カルーセル動作:**
- 右から左へゆっくり自動スクロール（CSS animation または JS）
- ホバー時に一時停止
- v0.1 ではモックデータで表示
- v0.2 以降で実際の検索結果をリアルタイム反映

### フッターページ

NANI（https://nani.now/ja）のサブページ構成を参考にする。
各ページは共通レイアウト（Header + Footer）を継承し、コンテンツ部分だけが変わるシンプルな構成。

| ページ | パス | 形式・内容 |
|---|---|---|
| 利用規約 | `/terms` | タブ切り替え式（利用規約 / プライバシーポリシー）。長文テキスト。 |
| 料金プラン | `/pricing` | 無料 / Pro の 2 カラム比較カード。各プランの機能一覧をチェックマーク付きで表示。 |
| サービスについて | `/about` | tada のコンセプト・開発の想いをストーリー形式で記載。 |
| 更新情報 | `/changelog` | 日付降順のリリースノート一覧。各エントリにバージョンと概要。 |
| FAQ | `/faq` | カテゴリ別アコーディオン（サービスについて / 料金・お支払い / その他）。トップページにも一部抜粋を表示。 |
| お問い合わせ | `/contact` | メールアドレス表示 + メールテンプレート提供（v0.1 はフォームではなくメールリンク方式で十分）。 |

**料金プランページ (`/pricing`) の詳細:**
```
┌─────────────────────┐  ┌─────────────────────┐
│  無料プラン           │  │  PRO（推奨）         │ ← 枠線 or 色で強調
│  ¥0 / 月             │  │  ¥980 / 月           │
│                     │  │                     │
│  ✓ 1日5回まで検索    │  │  ✓ 1日100回まで検索   │
│  ✓ sonarモデル       │  │  ✓ sonar-proモデル    │
│  ✓ ストリーミング表示 │  │    (30回まで)        │
│                     │  │  ✓ sonarモデル        │
│                     │  │    (31〜100回)       │
│                     │  │  ✓ ストリーミング表示  │
│                     │  │                     │
│  [無料ではじめる]     │  │  [Proプランをはじめる] │
└─────────────────────┘  └─────────────────────┘
```

**FAQ ページ (`/faq`) の詳細:**
- アコーディオン形式（クリックで開閉）
- カテゴリ分け: 「サービスについて」「料金・お支払いについて」「その他」
- トップページの FAQ セクションには主要 3〜5 問を抜粋し、「その他のよくある質問 >」リンクで `/faq` に誘導

---

## UX 方針

### 分析フェーズ → 構造化結果への切り替え

検索送信後、2 段階の UX で体感待ち時間を軽減する：

```
ユーザーが送信
  │
フェーズ 1: 分析中（analyzing）
  │ 各ステップが 800ms 間隔でフェードイン表示
  │ 「前世代モデルの情報を検索しています...」
  │ 「Leica M10 の発売時価格を取得 — 100万円（2017年）」
  │ ...
  │ 全ステップ完了後 600ms
  │
フェーズ 2: 結果表示（done）
  │ 構造化された UI コンポーネントに一気に切り替わる
  │ （マークダウン的なテキスト表示ではなく、カード・スライダー・グリッドで構成）
```

**重要:** 結果はマークダウンテキストの流し込みではなく、構造化された UI コンポーネント（カード・バー・グリッド）として表示する。「AIが作りました感」を避け、プロダクトとしての洗練さを保つ。

### ストリーミング表示（v0.1 で実装）

v0.1 では Perplexity Sonar API のレスポンスを Vercel AI SDK `streamText` でストリーミング取得するが、表示形式は上記の 2 段階 UX を維持する。API からデータを受信する過程でステップを表示し、全データ取得完了後に構造化カードに切り替える。

### クライアント側リアルタイム再計算

結果表示後、以下の操作でクライアント側のみで再計算が行われる（API 再呼び出しなし）：
- **年数スライダー** — 1〜20年の range input。変更すると推定売却価格・実質コスト・年間コストが即時更新。
- **価格編集** — 購入価格・推定売却価格を手動入力可能。変更すると実質コスト・年間コストが即時更新。
- **リセット** — 「AIの算出結果に戻す」ボタンで手動編集をキャンセルし、AI 算出値に復帰。

---

## 現在の状態（2026-03-15 時点）

| 項目 | 状態 |
|---|---|
| 環境構築 | 完了（Next.js + Supabase + Vercel + GitHub 連携済み） |
| v0.0 UI プロトタイプ | 完了（`prototype/ui` ブランチ。3回のレビューを経て UI 構造確定） |
| v0.0 Vercel デプロイ | 完了（プレビュー URL で動作確認済み） |
| Perplexity API キー | **未取得（v0.1 で必要。v0.0 では不要）** |

### 次にやるべきこと

1. **Perplexity API キーを取得** → https://docs.perplexity.ai
2. **v0.1 MVP の実装** — `dev` ブランチで API 連携して「本当に動くもの」を作る
3. v0.0 の UI 構造は確定済み。v0.1 では本ドキュメントの v0.0 セクションを「説明書」として参照し、API 連携版を構築する
